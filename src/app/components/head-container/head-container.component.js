import controller from './head-container.controller';
import template from './head-container.template.html';
import './head-container.css';

const headContainer = {
  template,
  controller,
  // bindings: {},
};

export default headContainer;
