import controller from './task-item.controller';
import template from './task-item.template.html';
import './task-item.css';

const taskItem = {
  template,
  controller,
  // bindings: {},
};

export default taskItem;
